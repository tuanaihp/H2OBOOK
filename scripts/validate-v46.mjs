import fs from 'node:fs';import path from 'node:path';const root=process.cwd();const required=['packages/automation-core/src/csv.ts','packages/automation-core/src/template.ts','packages/automation-core/src/generate.ts','app/bulk-publishing/page.tsx','app/api/bulk/source/route.ts','supabase/migrations/0012_h2obook_v46_data_automation.sql'];const missing=required.filter(file=>!fs.existsSync(path.join(root,file)));if(missing.length){console.error('Missing 4.6 files',missing);process.exit(1)}const core=fs.readFileSync(path.join(root,'packages/automation-core/src/template.ts'),'utf8');for(const marker of ['findTemplateFields','resolveTemplate','validateRows'])if(!core.includes(marker))throw new Error(`Missing ${marker}`);console.log(`H2OBOOK 4.6 Data Automation validation passed (${required.length} files).`);
