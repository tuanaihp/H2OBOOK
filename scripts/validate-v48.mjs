import fs from 'node:fs';import path from 'node:path';const root=process.cwd();const required=['packages/education-core/src/index.ts','app/class-view/page.tsx','app/remix/[bookId]/page.tsx','components/reader/accessibility-dock.tsx','app/api/remixes/route.ts','supabase/migrations/0014_h2obook_v48_education_collaboration.sql'];const missing=required.filter(file=>!fs.existsSync(path.join(root,file)));if(missing.length){console.error('Missing 4.8 files',missing);process.exit(1)}const core=fs.readFileSync(path.join(root,'packages/education-core/src/index.ts'),'utf8');for(const marker of ['remixProgress','classCellStatus','auditReadingOrder'])if(!core.includes(marker))throw new Error(`Education core missing ${marker}`);console.log(`H2OBOOK 4.8 Education validation passed (${required.length} files).`);
