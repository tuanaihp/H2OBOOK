import fs from 'node:fs';import path from 'node:path';const root=process.cwd();const required=['packages/growth-reader-core/src/index.ts','components/reader/growth-layer.tsx','app/growth-reader/page.tsx','app/api/reader/leads/route.ts','app/api/reader/embed-authorize/route.ts','supabase/migrations/0013_h2obook_v47_growth_reader.sql'];const missing=required.filter(file=>!fs.existsSync(path.join(root,file)));if(missing.length){console.error('Missing 4.7 files',missing);process.exit(1)}const core=fs.readFileSync(path.join(root,'packages/growth-reader-core/src/index.ts'),'utf8');for(const marker of ['evaluateReaderAccess','normalizeDomain','captureUtm'])if(!core.includes(marker))throw new Error(`Growth core missing ${marker}`);const reader=fs.readFileSync(path.join(root,'app/reader/[slug]/page.tsx'),'utf8');if(!reader.includes('GrowthLayer'))throw new Error('Reader does not include GrowthLayer');console.log(`H2OBOOK 4.7 Growth Reader validation passed (${required.length} files).`);
