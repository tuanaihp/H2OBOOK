import JSZip from "jszip";
import type { BookDocument } from "@h2obook/content-core";
import { renderSemanticHtml } from "./html";
import type { ExportProfile } from "./types";
export async function buildScormPackage(document:BookDocument,profile:ExportProfile,version:"1.2"|"2004"="1.2"){const zip=new JSZip();const api=version==="1.2"?"LMSInitialize":"Initialize";const finish=version==="1.2"?"LMSFinish":"Terminate";const html=renderSemanticHtml(document,profile).replace("</body>",`<script>function findAPI(w){for(let i=0;i<8&&w;i++,w=w.parent){if(w.API||w.API_1484_11)return w.API||w.API_1484_11}return null}const api=findAPI(window);api?.${api}("");window.addEventListener("beforeunload",()=>api?.${finish}(""));</script></body>`);zip.file("index.html",html);zip.file("imsmanifest.xml",version==="1.2"?`<?xml version="1.0"?><manifest identifier="h2obook-${document.id}" version="1.2"><organizations default="ORG"><organization identifier="ORG"><title>${document.title}</title><item identifier="ITEM" identifierref="RES"><title>${document.title}</title></item></organization></organizations><resources><resource identifier="RES" type="webcontent" adlcp:scormtype="sco" href="index.html" xmlns:adlcp="http://www.adlnet.org/xsd/adlcp_rootv1p2"><file href="index.html"/></resource></resources></manifest>`:`<?xml version="1.0"?><manifest xmlns="http://www.imsglobal.org/xsd/imscp_v1p1" xmlns:adlcp="http://www.adlnet.org/xsd/adlcp_v1p3" identifier="h2obook-${document.id}"><organizations default="ORG"><organization identifier="ORG"><title>${document.title}</title><item identifier="ITEM" identifierref="RES"><title>${document.title}</title></item></organization></organizations><resources><resource identifier="RES" type="webcontent" adlcp:scormType="sco" href="index.html"><file href="index.html"/></resource></resources></manifest>`);return zip.generateAsync({type:"uint8array",compression:"DEFLATE"});}
