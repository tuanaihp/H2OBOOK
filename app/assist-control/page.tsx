"use client";
import {AppShell} from "@/components/layout/app-shell";import {defaultAssistPolicy,type AssistPolicy,type AssistTask} from "@h2obook/optional-assist-core";import {CircleDollarSign,CloudOff,Database,Save,ShieldCheck,Sparkles} from "lucide-react";import {useEffect,useState} from "react";
const KEY='h2obook-optional-assist-policy';const tasks:AssistTask[]=['outline','rewrite','quiz','summary','brand_copy','translate','accessibility'];
export default function AssistControlPage(){const[policy,setPolicy]=useState<AssistPolicy>(defaultAssistPolicy);const[status,setStatus]=useState('AI đang tắt mặc định');useEffect(()=>{try{setPolicy({...defaultAssistPolicy,...JSON.parse(localStorage.getItem(KEY)??'{}')});}catch{}},[]);const patch=(value:Partial<AssistPolicy>)=>setPolicy(current=>({...current,...value}));const save=()=>{localStorage.setItem(KEY,JSON.stringify(policy));setStatus('Đã lưu policy local. Production policy được lưu trong PostgreSQL.');};return <AppShell><div className="page-header"><div><span className="eyebrow">OPTIONAL AI CONTROL</span><h1>AI là trợ lý, không phải nền móng.</h1><p>Ngân sách bằng 0 hoặc Gateway mất kết nối: hệ thống tự dùng Smart Core Local.</p></div><span className="core-status-pill"><ShieldCheck/>Core độc lập AI</span></div><section className="assist-policy-banner"><CloudOff/><div><strong>Fail-open về Local Core</strong><p>Mọi request AI lỗi đều trả kết quả local thay vì chặn người dùng.</p></div><Database/><div><strong>Cache kết quả</strong><p>Không trả phí lại cho cùng một tác vụ và nội dung.</p></div></section><div className="assist-policy-grid"><section className="section-card"><div className="section-head"><div><h2>Trạng thái</h2><p>AI mặc định tắt theo workspace.</p></div><Sparkles/></div><label className="switch-row"><span><strong>Cho phép AI ngoài</strong><small>Chỉ bật sau khi chủ workspace xác nhận.</small></span><input type="checkbox" checked={policy.enabled} onChange={e=>patch({enabled:e.target.checked})}/></label><label>Chế độ mặc định<select value={policy.defaultMode} onChange={e=>patch({defaultMode:e.target.value as AssistPolicy['defaultMode']})}><option value="local">Smart Core Local</option><option value="external">AI ngoài khi đủ điều kiện</option></select></label><label className="switch-row"><span><strong>Cache kết quả</strong><small>Giảm token và chi phí lặp lại.</small></span><input type="checkbox" checked={policy.cacheEnabled} onChange={e=>patch({cacheEnabled:e.target.checked})}/></label></section><section className="section-card"><div className="section-head"><div><h2>Ngân sách</h2><p>Hard limit theo tháng.</p></div><CircleDollarSign/></div><label>Ngân sách tháng (USD)<input type="number" min="0" step="1" value={policy.monthlyBudgetUsd} onChange={e=>patch({monthlyBudgetUsd:Number(e.target.value)})}/></label><div className="assist-budget"><span><i style={{width:`${Math.min(100,policy.monthlyBudgetUsd?policy.spentUsd/policy.monthlyBudgetUsd*100:0)}%`}}/></span><p>Đã dùng ${policy.spentUsd.toFixed(2)} / ${policy.monthlyBudgetUsd.toFixed(2)}</p></div><label>Ký tự tối đa mỗi request<input type="number" min="1000" max="100000" value={policy.maxPromptCharacters} onChange={e=>patch({maxPromptCharacters:Number(e.target.value)})}/></label></section><section className="section-card"><div className="section-head"><div><h2>Tác vụ được phép</h2><p>Tắt riêng từng loại công việc.</p></div><ShieldCheck/></div><div className="assist-task-list">{tasks.map(task=><button key={task} className={policy.allowedTasks.includes(task)?'active':''} onClick={()=>patch({allowedTasks:policy.allowedTasks.includes(task)?policy.allowedTasks.filter(item=>item!==task):[...policy.allowedTasks,task]})}>{task}</button>)}</div><label className="switch-row"><span><strong>Cho gửi ảnh</strong><small>Mặc định không gửi asset ra ngoài.</small></span><input type="checkbox" checked={policy.allowImageInput} onChange={e=>patch({allowImageInput:e.target.checked})}/></label><label className="switch-row"><span><strong>Nguồn bên ngoài</strong><small>Mặc định chỉ dùng nội dung người dùng chọn.</small></span><input type="checkbox" checked={policy.allowExternalSources} onChange={e=>patch({allowExternalSources:e.target.checked})}/></label></section></div><div className="growth-savebar"><span>{status}</span><button className="btn btn-primary" onClick={save}><Save/>Lưu AI Policy</button></div></AppShell>}
