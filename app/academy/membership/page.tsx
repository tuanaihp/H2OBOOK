import Link from "next/link";
import { ArrowRight, CheckCircle2, ShieldCheck, Sparkles } from "lucide-react";
import { PublicShell } from "@/components/marketing/public-shell";
import { AcademyEnrollmentCard } from "@/components/academy/enrollment-card";
import { formatVnd, membershipPlans } from "@/lib/public-site/content";
export const metadata={title:"Membership H2OBOOK Academy"};
export default function MembershipPublicPage(){return <PublicShell><section className="h2o-public-subhero membership"><div className="h2o-public-container"><span>H2OBOOK MEMBERSHIP</span><h1>Một tài khoản, một hệ sinh thái học tập và phát triển nghề.</h1><p>Chọn mức đồng hành phù hợp: đọc sách, học kỹ năng hoặc xây hệ thống kinh doanh Beauty.</p></div></section><section className="h2o-public-section"><div className="h2o-public-container"><div className="h2o-membership-grid catalog">{membershipPlans.map(plan=><article key={plan.id} className={plan.featured?"featured":""}>{plan.featured&&<span className="h2o-plan-popular">Được lựa chọn nhiều</span>}<small>{plan.name}</small><h3>{formatVnd(plan.price)}<span>/{plan.period}</span></h3><p>{plan.description}</p><ul>{plan.features.map(item=><li key={item}><CheckCircle2/>{item}</li>)}</ul><Link href={`/academy/membership?plan=${plan.id}#academy-enrollment`}>Bắt đầu với gói này <ArrowRight/></Link></article>)}</div><div className="h2o-membership-guarantee"><ShieldCheck/><div><strong>Dữ liệu học tập thuộc về bạn.</strong><p>Core học tập, reader, ghi chú, quiz và tiến độ vẫn hoạt động khi AI tắt. AI chỉ là lớp hỗ trợ được kiểm soát.</p></div></div></div></section><AcademyEnrollmentCard targetType="membership" initialSlug="academy" options={membershipPlans.map(plan=>({slug:plan.id,label:plan.name,price:plan.price}))}/><section className="h2o-public-final-cta compact"><div className="h2o-public-container"><div><Sparkles/><span>START YOUR KNOWLEDGE JOURNEY</span><h2>Bắt đầu từ thư viện hôm nay.<br/><em>Nâng cấp khi bạn cần thêm hỗ trợ.</em></h2><Link className="h2o-public-primary large" href="#academy-enrollment">Tạo tài khoản học viên <ArrowRight/></Link></div></div></section></PublicShell>}
